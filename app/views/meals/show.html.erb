<p style="color: green"><%= notice %></p>

<%= render @meal %>

<div class="flex gap-2 my-4">
  <%= link_to "Edit", edit_meal_path(@meal), class: "btn btn-primary" %>

  <%= button_to "Delete", @meal, method: :delete, class: "btn btn-error" %>
</div>
